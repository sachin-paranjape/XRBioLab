/**
 * Print the disconnection message to the console and then exit
 *
 * @param message
 */
export default function clientDisconnect(message) {
    const { exitCode } = message;
    process.exit(exitCode !== null && exitCode !== void 0 ? exitCode : 0);
}
//# sourceMappingURL=client-disconnect.js.map